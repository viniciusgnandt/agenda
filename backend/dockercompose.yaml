version: "3.9"

services:
  backend:
    build: .
    container_name: agenda-backend
    ports:
      - "7001:3000"
    env_file:
      - .env
    networks:
      - agenda-net
    volumes:
      - .:/app:delegated
      - /app/node_modules

networks:
  agenda-net:
    external: true